# e01 · 配置 git 别名（git graph）

给 git 配置一个 `graph` 别名，用 `git log --all --graph --decorate --oneline`
以图形化方式查看提交历史。

- 运行：`bash demo.sh`
- 关键命令：`git config --global alias.graph "log --all --graph --decorate --oneline"`
