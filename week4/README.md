# 第四周课程练习材料

本仓库收录课程第 13-16 题与实例 1-11 的练习题目、操作说明与实验截图。

## q13 - q16
- q13：建立可执行的本地质量门禁（ruff + pytest + check.sh）
- q14：Make 只重建真正受影响的产物
- q15：把本地 API 数据转换为可读报告（curl + jq + Markdown）
- q16：修复并交付一个陌生的小型工具仓库

## 实例 1 - 11
- 实例1：正则表达式与 grep 查找危险代码（semgrep 对比）
- 实例2：正则提取 JSON 中的 name 字段
- 实例3：black + flake8 + pre-commit 钩子配置
- 实例4：pytest 单元测试与 HTML 覆盖率报告
- 实例5：持续集成流水线（push 三级质量门禁）
- 实例6：正则查找替换（Markdown 项目符号）
- 实例7：Makefile 的 clean 构建目标
- 实例8：Cargo 依赖版本约束语法
- 实例9：pre-commit 钩子构建检查
- 实例10：shellcheck 静态检查 shell 脚本
- 实例11：proselint 检查 Markdown 文档
